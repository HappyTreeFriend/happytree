{% extends "base.html" %}

{% block page %}╡И©╢ндуб{% endblock %}

{% block content %}
	<section id="article_id_{{ article.id }}">
		<p><strong>{{ article.title }}</strong></p>
		<p><strong>{{ article.content }}</strong></p>
		<p><small>{{ article.tag }}</small></p>
	</section>
	<section id="comm">
	<div id="comm_id_{{ comment.id }}">
		<p>{{ comment.author }}</p>
		<p>{{ comment.content }}</p>
	</div>
	</section>
{% endblock %}	
